command-start = Hello!

command-backgrounds = Working with uploaded backgrounds

command-templates = Working with schedule templates

command-elements = Working with overlay images (for administrators)

command-upload = Upload a new background

command-create = Create a schedule

command-settings = Bot settings

command-help = About the bot

command-grant = Assign administrator (for administrators)
    .unparsed = Command usage: <code>/grant 12345</code> (use account ID) or <code>/grant</code>

command-revoke = Revoke administrator (for administrators)
    .unparsed = Command usage: <code>/revoke 12345</code> (use account ID) or <code>/revoke</code>

command-ban = Ban user
    .unparsed = Command usage: <code>/ban 12345</code> (use account ID) or <code>/ban</code>

command-unban = Unban user
    .unparsed = Command usage: <code>/unban 12345</code> (use account ID) or <code>/unban</code>
